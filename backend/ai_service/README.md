# AI Service

FastAPI service for chat, RAG, PDF indexing, tool calls, complaints, and policy requests.

Run from the repo root:

```powershell
python -m pip install -r backend/ai_service/requirements.txt
npm run dev:ai
```
